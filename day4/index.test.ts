import fs from 'fs';
import { describe, it, assert } from 'vitest';
import day4 from './index';

const testInput = ``.trim();

const fileContents = fs.readFileSync('./day3/input.txt', 'utf8')
const input = fileContents.trim();

describe('Day 4', () => {
   
});